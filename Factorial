
def fact (x):
    for j in range(x-1,0,-1):
        x=x*j
    return(x)
num = int(input("Enter a number "))
print (fact(num))

def fact2 (x):
    if x==1:
        return (1)
    else:
        return (x*fact2(x-1))

num2 =int(input("Enter a number "))
print (fact2(num2))